{% extends 'base.html.twig' %}

{% block body %}
    <h1>Entidadreguladora</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ entidadReguladora.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ entidadReguladora.nombre }}</td>
            </tr>
            <tr>
                <th>Detalle</th>
                <td>{{ entidadReguladora.detalle }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('entidadreguladora_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('entidadreguladora_edit', { 'id': entidadReguladora.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
