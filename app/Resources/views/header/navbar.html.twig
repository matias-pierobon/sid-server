<nav class="navbar fixed-top navbar-inverse navbar-toggleable-md">

    <a class="navbar-brand" href="#">
        {% include 'header/icon.svg.twig' %}
        Abipon
    </a>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#mainPage .sidebar" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
    </button>

    <div class="mr-auto">
    </div>

    <ul class="navbar-nav">
        {% if app.user %}
            <li class="nav-item dropdown">
                <a class="nav-link dropdown-toggle active" id="dropdownMenuButton" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                    <i class="fa fa-user-circle"></i>
                </a>
                <div class="dropdown-menu dropdown-menu-right" aria-labelledby="dropdownMenuButton">
                    <div class="dropdown-item" href="#">{{ app.user.lastname }}, {{ app.user.name }}</div>
                    <a class="dropdown-item" href="#"><i class="fa fa-cog"></i> Perfil</a>
                    <div class="dropdown-divider"></div>
                    <a class="dropdown-item" href="{{ path('logout') }}"><i class="fa fa-sign-out"></i> Cerrar Sesión</a>
                </div>
            </li>
        {% else %}
        <li class="nav-item">
            <a class="nav-link" href="{{ path('login') }}"><i class="fa fa-sign-in"></i> Login</a>
        </li>
        {% endif %}
    </ul>
</nav>