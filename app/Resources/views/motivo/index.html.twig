{% extends 'base.html.twig' %}

{% block body %}
    <h1>Motivos list</h1>

    <table>
        <thead>
            <tr>
                <th>Id</th>
                <th>Nombre</th>
                <th>Detalle</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for motivo in motivos %}
            <tr>
                <td><a href="{{ path('motivo_show', { 'id': motivo.id }) }}">{{ motivo.id }}</a></td>
                <td>{{ motivo.nombre }}</td>
                <td>{{ motivo.detalle }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('motivo_show', { 'id': motivo.id }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('motivo_edit', { 'id': motivo.id }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('motivo_new') }}">Create a new motivo</a>
        </li>
    </ul>
{% endblock %}
